To generate a git tag list, run:

```sh
bun src/__tests__/__fixtures__/git-tag/generate.ts <repoName> <rootDir> <prefix>
```

For example:

```sh
bun src/__tests__/__fixtures__/git-tag/generate.ts "wxt" "/path/to/wxt" "wxt-v"
```
